const igniteEngine = (developerName) => {
    const message = `SYSTEM ONLINE: Good morning, ${developerName}. The Away-Team protocol is active.`;
    return message;
  };
  
  console.log(igniteEngine("SG"));

 
 